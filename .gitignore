# Dependencies
node_modules/

# Nuxt build artifacts (keep .output/ tracked for zero-build deploys)
.nuxt/
.nitro/
# .output/ — intentionally NOT ignored so cPanel can serve without rebuilding

# Environment — NEVER commit real credentials
.env
.env.*
!.env.example

# Uploads (user-generated files, not code)
public/uploads/

# System
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Editor
.vscode/
.idea/
